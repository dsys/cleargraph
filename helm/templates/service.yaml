apiVersion: v1
kind: Service
metadata:
  name: {{ template "eth-explorer.fullname" . }}
  labels:
    app: {{ template "eth-explorer.name" . }}
    chart: {{ template "eth-explorer.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "eth-explorer.name" . }}
    release: {{ .Release.Name }}
